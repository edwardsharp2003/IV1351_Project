# This file is a template. Copy it to a new file named .env and fill in
# your local PostgreSQL credentials before running the application.

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_NAME=iv1351t3